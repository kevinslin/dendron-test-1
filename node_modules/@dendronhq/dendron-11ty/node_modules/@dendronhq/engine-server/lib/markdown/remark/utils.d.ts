import { DendronError, NoteChangeEntry, NotePropsV2 } from "@dendronhq/common-all";
import { Processor } from "unified";
import { Node } from "unist";
import { VFile } from "vfile";
import { WikiLinkProps } from "../../topics/markdown";
export declare const ALIAS_DIVIDER = "|";
export declare function addError(proc: Processor, err: DendronError): void;
export declare function getNoteOrError(notes: NotePropsV2[], hint: any): {
    error: DendronError | undefined;
    note: undefined | NotePropsV2;
};
export declare class LinkUtils {
    static isAlias(link: string): boolean;
    static parseAliasLink(link: string): {
        alias: string;
        value: string;
    };
    static parseLink(linkMatch: string): WikiLinkProps;
}
export declare class RemarkUtils {
    static h1ToTitle(note: NotePropsV2, changes: NoteChangeEntry[]): (this: Processor) => (tree: Node, _vfile: VFile) => void;
    static h1ToH2(note: NotePropsV2, changes: NoteChangeEntry[]): (this: Processor) => (tree: Node, _vfile: VFile) => void;
}
